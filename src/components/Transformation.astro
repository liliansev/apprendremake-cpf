---
// Transformation section - "The Gap" - Before/After emotional journey
import { Icon } from 'astro-icon/components';

const transformations = [
  {
    before: {
      icon: "lucide:search",
      title: "Le labyrinthe YouTube",
      description: "4 heures perdues à chercher une solution qui ne correspond pas exactement à ton cas."
    },
    after: {
      icon: "lucide:map",
      title: "La méthode structurée",
      description: "Tu construis 3x plus vite avec une roadmap claire. Plus de doutes, juste de l'exécution."
    }
  },
  {
    before: {
      icon: "lucide:bug",
      title: "L'enfer du débuggage",
      description: "Un scénario qui s'arrête, une erreur 400 inconnue... et personne pour t'aider."
    },
    after: {
      icon: "lucide:zap",
      title: "Le réflexe chirurgical",
      description: "Tu résous n'importe quelle erreur en 2 minutes chrono. Tes automatisations sont fiables à 100%."
    }
  },
  {
    before: {
      icon: "lucide:wallet",
      title: "La taxe \"Expert\"",
      description: "Devoir payer un consultant 100€/h dès que tu veux ajouter une étape complexe."
    },
    after: {
      icon: "lucide:crown",
      title: "L'autonomie totale",
      description: "0€ dépensé en consultants. Tu es le seul maître à bord de tes systèmes complexes."
    }
  },
  {
    before: {
      icon: "lucide:alert-triangle",
      title: "La boule au ventre",
      description: "La peur de \"tout casser\" dès que tu touches à un module actif."
    },
    after: {
      icon: "lucide:trending-up",
      title: "La sérénité du scale",
      description: "Tes systèmes tiennent la charge. Tu peux multiplier tes clients sans stress."
    }
  },
  {
    before: {
      icon: "lucide:frown",
      title: "La corvée Make",
      description: "Construire un scénario te semble fastidieux, tu procrastines sur tes automatisations."
    },
    after: {
      icon: "lucide:sparkles",
      title: "Le plaisir de créer",
      description: "Construire sur Make devient un jeu de construction gratifiant et serein."
    }
  },
  {
    before: {
      icon: "lucide:clock",
      title: "Le temps qui file",
      description: "Des heures perdues chaque semaine sur des tâches manuelles répétitives."
    },
    after: {
      icon: "lucide:banknote",
      title: "La machine à résultats",
      description: "Tes processus tournent en pilote automatique. Ton business grandit pendant que tu dors."
    }
  }
];
---

<section class="py-16 md:py-24 bg-zinc-50/50 overflow-hidden">
  <div class="max-w-6xl mx-auto px-4 md:px-6">
    <!-- Header -->
    <div class="text-center mb-12 md:mb-16">
      <p class="text-violet-600 text-xs font-medium tracking-wider uppercase mb-3">La transformation</p>
      <h2 class="text-2xl md:text-4xl font-semibold text-zinc-900 mb-4">
        Arrêtez de subir Make.<br class="hidden sm:block" />
        <span class="text-violet-600">Commencez à dompter l'outil.</span>
      </h2>
      <p class="text-zinc-500 max-w-xl mx-auto">
        Ce n'est pas juste une formation. C'est un changement d'état d'esprit.
      </p>
    </div>

    <!-- Desktop: Split screen -->
    <div class="hidden md:grid md:grid-cols-2 gap-0 rounded-2xl overflow-hidden shadow-lg border border-zinc-200 bg-white">
      <!-- Before column -->
      <div class="p-8 lg:p-10 border-r border-zinc-100">
        <div class="flex items-center gap-3 mb-8">
          <div class="w-10 h-10 rounded-full bg-red-500/10 flex items-center justify-center">
            <Icon name="lucide:x" class="w-5 h-5 text-red-500" />
          </div>
          <div>
            <p class="text-xs font-medium text-red-500 uppercase tracking-wider">Avant</p>
            <p class="text-lg font-semibold text-zinc-900">Seul face à la machine</p>
          </div>
        </div>

        <div class="space-y-6">
          {transformations.map((item) => (
            <div class="group">
              <div class="flex items-start gap-3">
                <div class="w-7 h-7 rounded-lg bg-red-500/10 flex items-center justify-center shrink-0 mt-0.5">
                  <Icon name={item.before.icon} class="w-4 h-4 text-red-500" />
                </div>
                <div>
                  <h4 class="text-base font-semibold text-zinc-900 mb-1">{item.before.title}</h4>
                  <p class="text-sm text-zinc-500 leading-relaxed">{item.before.description}</p>
                </div>
              </div>
            </div>
          ))}
        </div>
      </div>

      <!-- After column -->
      <div class="p-8 lg:p-10 bg-violet-50/50">
        <div class="flex items-center gap-3 mb-8">
          <div class="w-10 h-10 rounded-full bg-violet-500/10 flex items-center justify-center">
            <Icon name="lucide:check" class="w-5 h-5 text-violet-600" />
          </div>
          <div>
            <p class="text-xs font-medium text-violet-600 uppercase tracking-wider">Après</p>
            <p class="text-lg font-semibold text-zinc-900">Le mindset d'Expert</p>
          </div>
        </div>

        <div class="space-y-6">
          {transformations.map((item) => (
            <div class="group">
              <div class="flex items-start gap-3">
                <div class="w-7 h-7 rounded-lg bg-violet-500/10 flex items-center justify-center shrink-0 mt-0.5">
                  <Icon name={item.after.icon} class="w-4 h-4 text-violet-600" />
                </div>
                <div>
                  <h4 class="text-base font-semibold text-zinc-900 mb-1">{item.after.title}</h4>
                  <p class="text-sm text-zinc-500 leading-relaxed">{item.after.description}</p>
                </div>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>

    <!-- Mobile: Stacked cards -->
    <div class="md:hidden space-y-4">
      {transformations.map((item) => (
        <div class="rounded-xl overflow-hidden shadow-md border border-zinc-200 bg-white">
          <!-- Before -->
          <div class="p-4 border-b border-zinc-100">
            <div class="flex items-start gap-3">
              <div class="w-7 h-7 rounded-lg bg-red-500/10 flex items-center justify-center shrink-0 mt-0.5">
                <Icon name={item.before.icon} class="w-4 h-4 text-red-500" />
              </div>
              <div>
                <p class="text-[10px] font-medium text-red-500 uppercase tracking-wider mb-0.5">Avant</p>
                <h4 class="text-sm font-semibold text-zinc-900 mb-1">{item.before.title}</h4>
                <p class="text-xs text-zinc-500 leading-relaxed">{item.before.description}</p>
              </div>
            </div>
          </div>
          <!-- Arrow -->
          <div class="py-1.5 flex justify-center bg-zinc-50">
            <Icon name="lucide:arrow-down" class="w-4 h-4 text-violet-500" />
          </div>
          <!-- After -->
          <div class="p-4 bg-violet-50/50">
            <div class="flex items-start gap-3">
              <div class="w-7 h-7 rounded-lg bg-violet-500/10 flex items-center justify-center shrink-0 mt-0.5">
                <Icon name={item.after.icon} class="w-4 h-4 text-violet-600" />
              </div>
              <div>
                <p class="text-[10px] font-medium text-violet-600 uppercase tracking-wider mb-0.5">Après</p>
                <h4 class="text-sm font-semibold text-zinc-900 mb-1">{item.after.title}</h4>
                <p class="text-xs text-zinc-500 leading-relaxed">{item.after.description}</p>
              </div>
            </div>
          </div>
        </div>
      ))}
    </div>

    <!-- Bottom hook -->
    <div class="mt-10 text-center">
      <p class="text-lg text-zinc-600">
        <span class="font-semibold text-zinc-900">Devenez l'architecte de votre automatisation.</span> Votre business devient <span class="text-violet-600 font-medium">une machine autonome</span>.
      </p>
    </div>
  </div>
</section>
