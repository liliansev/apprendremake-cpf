---
// Programme section - Conversion optimized with best practices
const phases = [
  {
    id: 1,
    name: "Fondamentaux",
    color: "violet",
    outcome: "Créez vos premiers scénarios automatisés",
    chapters: [
      { num: "01", title: "Le fonctionnement de Make", lessons: 10 },
      { num: "02", title: "La programmation visuelle", lessons: 6 },
      { num: "03", title: "Formules Basiques", lessons: 6 }
    ]
  },
  {
    id: 2,
    name: "Maîtrise",
    color: "blue",
    outcome: "Manipulez les données complexes",
    chapters: [
      { num: "04", title: "Maîtrise des dates", lessons: 7 },
      { num: "05", title: "Les booléans", lessons: 5 },
      { num: "06", title: "Bonnes pratiques", lessons: 9 }
    ]
  },
  {
    id: 3,
    name: "Intégrations",
    color: "emerald",
    outcome: "Connectez n'importe quelle API",
    chapters: [
      { num: "07", title: "Les APIs", lessons: 7 },
      { num: "08", title: "Les webhooks", lessons: 9 },
      { num: "09", title: "Boucles & Arrays", lessons: 12 },
      { num: "10", title: "Le JSON", lessons: 5 }
    ]
  },
  {
    id: 4,
    name: "Expert",
    color: "amber",
    outcome: "Créez des automatisations avancées",
    chapters: [
      { num: "12", title: "Les aggrégateurs", lessons: 10 },
      { num: "13", title: "Fonctions complexes", lessons: 4 },
      { num: "14", title: "Datastores", lessons: 9 }
    ]
  }
];

const finalPhase = {
  id: 5,
  name: "Projet Final",
  color: "rose",
  outcome: "Mettez tout en pratique avec un projet complet de A à Z",
  chapters: [
    { num: "16", title: "Projet complet", lessons: 4 },
    { num: "17", title: "oAuth 2.0", lessons: 3 }
  ]
};

const totalLessons = phases.reduce((acc, phase) =>
  acc + phase.chapters.reduce((sum, ch) => sum + ch.lessons, 0), 0
) + finalPhase.chapters.reduce((sum, ch) => sum + ch.lessons, 0);

const totalChapters = phases.reduce((acc, phase) => acc + phase.chapters.length, 0) + finalPhase.chapters.length;
---

<section id="programme" class="py-16 md:py-24 bg-gradient-to-b from-white to-zinc-50">
  <div class="max-w-5xl mx-auto px-4 md:px-6">
    <!-- Header -->
    <div class="text-center mb-10">
      <p class="text-violet-600 text-xs font-medium tracking-wider uppercase mb-3">Programme complet</p>
      <h2 class="text-2xl md:text-4xl font-semibold text-zinc-900 mb-3">
        De débutant à expert Make
      </h2>
      <p class="text-zinc-500 text-sm max-w-lg mx-auto mb-6">
        Un parcours structuré en 5 phases pour vous rendre autonome. Chaque chapitre inclut exercices, corrections et quiz.
      </p>

      <!-- Stats pills -->
      <div class="flex flex-wrap items-center justify-center gap-2">
        <span class="inline-flex items-center gap-1.5 text-xs font-medium text-zinc-600 bg-white border border-zinc-200 px-3 py-1.5 rounded-full">
          <svg class="w-3.5 h-3.5 text-violet-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"></path>
          </svg>
          {totalChapters} chapitres
        </span>
        <span class="inline-flex items-center gap-1.5 text-xs font-medium text-zinc-600 bg-white border border-zinc-200 px-3 py-1.5 rounded-full">
          <svg class="w-3.5 h-3.5 text-violet-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.752 11.168l-3.197-2.132A1 1 0 0010 9.87v4.263a1 1 0 001.555.832l3.197-2.132a1 1 0 000-1.664z"></path>
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
          {totalLessons} leçons vidéo
        </span>
        <span class="inline-flex items-center gap-1.5 text-xs font-medium text-zinc-600 bg-white border border-zinc-200 px-3 py-1.5 rounded-full">
          <svg class="w-3.5 h-3.5 text-violet-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
          Exercices corrigés
        </span>
      </div>
    </div>

    <!-- Phases 1-4 Grid (2x2) -->
    <div class="grid sm:grid-cols-2 gap-4 mb-4">
      {phases.map((phase) => {
        const phaseTotal = phase.chapters.reduce((sum, ch) => sum + ch.lessons, 0);
        return (
          <div class={`relative p-5 rounded-2xl bg-white border border-zinc-200 hover:border-zinc-300 transition-colors`}>
            <!-- Phase indicator -->
            <div class="flex items-center gap-3 mb-4">
              <div class={`w-8 h-8 rounded-lg flex items-center justify-center text-sm font-bold ${
                phase.color === 'violet' ? 'bg-violet-100 text-violet-600' :
                phase.color === 'blue' ? 'bg-blue-100 text-blue-600' :
                phase.color === 'emerald' ? 'bg-emerald-100 text-emerald-600' :
                'bg-amber-100 text-amber-600'
              }`}>
                {phase.id}
              </div>
              <div>
                <h3 class="text-sm font-semibold text-zinc-900">{phase.name}</h3>
                <p class="text-[11px] text-zinc-400">{phaseTotal} leçons · {phase.chapters.length} chapitres</p>
              </div>
            </div>

            <!-- Outcome -->
            <p class="text-xs text-zinc-600 mb-4 leading-relaxed">{phase.outcome}</p>

            <!-- Chapters -->
            <div class="space-y-2">
              {phase.chapters.map((chapter) => (
                <div class="flex items-center gap-2 text-xs">
                  <span class={`w-5 h-5 rounded flex items-center justify-center text-[10px] font-mono ${
                    phase.color === 'violet' ? 'bg-violet-50 text-violet-500' :
                    phase.color === 'blue' ? 'bg-blue-50 text-blue-500' :
                    phase.color === 'emerald' ? 'bg-emerald-50 text-emerald-500' :
                    'bg-amber-50 text-amber-500'
                  }`}>{chapter.num}</span>
                  <span class="text-zinc-700 flex-grow">{chapter.title}</span>
                  <span class="text-zinc-400">{chapter.lessons}</span>
                </div>
              ))}
            </div>
          </div>
        );
      })}
    </div>

    <!-- Phase 5: Final Project (Full width highlight) -->
    <div class="relative p-6 rounded-2xl bg-gradient-to-r from-rose-50 to-orange-50 border border-rose-200">
      <div class="flex flex-col sm:flex-row sm:items-center gap-4">
        <!-- Left: Info -->
        <div class="flex-grow">
          <div class="flex items-center gap-3 mb-2">
            <div class="w-8 h-8 rounded-lg bg-rose-100 text-rose-600 flex items-center justify-center text-sm font-bold">
              5
            </div>
            <div>
              <h3 class="text-sm font-semibold text-zinc-900">{finalPhase.name}</h3>
              <p class="text-[11px] text-zinc-400">{finalPhase.chapters.reduce((sum, ch) => sum + ch.lessons, 0)} leçons · {finalPhase.chapters.length} chapitres</p>
            </div>
          </div>
          <p class="text-xs text-zinc-600">{finalPhase.outcome}</p>
        </div>

        <!-- Right: Chapters -->
        <div class="flex gap-3 sm:gap-4">
          {finalPhase.chapters.map((chapter) => (
            <div class="flex items-center gap-2 text-xs bg-white/60 px-3 py-2 rounded-lg">
              <span class="w-5 h-5 rounded bg-rose-100 text-rose-500 flex items-center justify-center text-[10px] font-mono">{chapter.num}</span>
              <span class="text-zinc-700">{chapter.title}</span>
            </div>
          ))}
        </div>
      </div>
    </div>

    <!-- CTA -->
    <div class="mt-10 text-center">
      <a
        href="#pricing"
        class="inline-flex items-center justify-center gap-2 bg-zinc-900 hover:bg-zinc-800 text-white text-sm font-medium px-8 py-3.5 rounded-xl transition-colors shadow-sm"
      >
        <span>Accéder à la formation complète</span>
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
        </svg>
      </a>
      <p class="text-xs text-zinc-400 mt-3">Accès à vie · Mises à jour incluses · Satisfait ou remboursé</p>
    </div>
  </div>
</section>
